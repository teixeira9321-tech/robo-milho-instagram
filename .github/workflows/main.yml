name: "🚀 Motor de Elite - Milho Premium V2"

on:
  schedule:
    # Horários Otimizados para o Brasil (UTC-3):
    # 10:00 UTC = 07:00 Brasil
    # 15:00 UTC = 12:00 Brasil
    # 22:00 UTC = 19:00 Brasil
    - cron: '0 10,15,22 * * *'
  
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Forçar execução manual?'
        required: false
        default: 'true'

# 🛡️ TRAVA DE SEGURANÇA
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  operacao-blindada:
    name: "🤖 Postagem e Organização"
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: "📂 Sincronizando Repositório"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: "🔍 Diagnóstico Pré-Voo"
        run: |
          echo "📂 Estado ATUAL dos arquivos (Antes da Postagem):"
          ls -R
          
      - name: "🐍 Setup Python Otimizado"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: "📦 Instalação de Bibliotecas Oficiais"
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade instagrapi requests google-generativeai pillow

      - name: "⚙️ Executando Motor de IA"
        env:
          INSTA_SESSION: ${{ secrets.INSTA_SESSION }}
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          PYTHONUNBUFFERED: 1 
          TZ: 'America/Sao_Paulo'
        run: |
          echo "🚀 Iniciando script principal..."
          python main.py

      - name: "💾 Salvamento Inteligente (A CORREÇÃO ESTÁ AQUI)"
        if: success() 
        run: |
          echo "⚙️ Configurando identidade do Robô..."
          git config --global user.name 'Milho Elite Bot'
          git config --global user.email 'bot@milhopremium.com'
          
          echo "🔄 Sincronizando antes de salvar (Evita conflito)..."
          git pull --rebase origin main || echo "⚠️ Nada para puxar, seguindo..."

          echo "📂 Registrando todas as mudanças (Inclusive Deleções)..."
          # CORREÇÃO: '-A' garante que o arquivo movido seja DELETADO da origem
          git add -A
          
          # Verifica se houve mudança real nos arquivos
          if git diff --staged --quiet; then
            echo "✅ Nenhuma alteração pendente (Talvez a pasta já estava vazia)."
          else
            git commit -m "🤖 [AUTO] Postagem realizada: Arquivo movido e deletado da origem"
            git push
            echo "✅ Alterações salvas! O vídeo original foi removido com sucesso."
          fi

      - name: "📊 Relatório Final"
        if: always()
        run: |
          echo "Status Final: ${{ job.status }}"
          echo "Finalizado em: $(date)"
